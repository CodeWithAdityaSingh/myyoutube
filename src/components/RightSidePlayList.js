import React from 'react'
import { useSelector } from 'react-redux'
import VideoCard from './VideoCard';

const RightSidePlayList = () => {

    // let selector = useSelector((store)=>store.search.videoContainerData )

    
  return (
    <div className='border-2'>
    
    </div>
  )
}

export default RightSidePlayList
